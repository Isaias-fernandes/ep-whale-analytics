(()=>{const $=s=>document.querySelector(s),fmt=(n,d=2)=>Number.isFinite(+n)?(+n).toLocaleString('pt-BR',{maximumFractionDigits:d}):'—';const CRYPTO=[['BTCUSDT','BTC'],['ETHUSDT','ETH'],['SOLUSDT','SOL'],['BNBUSDT','BNB'],['XRPUSDT','XRP'],['ADAUSDT','ADA'],['DOGEUSDT','DOGE'],['AVAXUSDT','AVAX'],['LINKUSDT','LINK'],['DOTUSDT','DOT'],['LTCUSDT','LTC'],['BCHUSDT','BCH'],['TRXUSDT','TRX'],['UNIUSDT','UNI'],['AAVEUSDT','AAVE'],['SUIUSDT','SUI'],['NEARUSDT','NEAR'],['APTUSDT','APT'],['ARBUSDT','ARB'],['OPUSDT','OP']];function ema(v,p){let k=2/(p+1),o=[v[0]];for(let i=1;i<v.length;i++)o.push(v[i]*k+o[i-1]*(1-k));return o}function rsi(v,p=7){if(v.length<=p)return NaN;let g=0,l=0;for(let i=1;i<=p;i++){let d=v[i]-v[i-1];d>=0?g+=d:l-=d}let ag=g/p,al=l/p;for(let i=p+1;i<v.length;i++){let d=v[i]-v[i-1];ag=(ag*(p-1)+Math.max(d,0))/p;al=(al*(p-1)+Math.max(-d,0))/p}return al?100-100/(1+ag/al):100}async function kl(sym,tf,limit=220){let r=await fetch(`https://api.binance.com/api/v3/klines?symbol=${sym}&interval=${tf}&limit=${limit}`,{cache:'no-store'});if(!r.ok)throw Error('Binance '+r.status);return(await r.json()).map(a=>({t:+a[0],o:+a[1],h:+a[2],l:+a[3],c:+a[4],v:+a[5]}))}function tfSignal(c){let close=c.map(x=>x.c),e20=ema(close,20),e30=ema(close,30),line=close.map((_,i)=>e20[i]-e30[i]),sig=ema(line,60),hist=line.map((x,i)=>x-sig[i]),h=hist.at(-1),prev=hist.at(-2),rv=rsi(close),p=close.at(-1),eps=Math.max(Math.abs(p)*0.00005,1e-12),dir=Math.abs(h)<=eps?'NEUTRAL':h>0?'BUY':'SELL',score=dir==='NEUTRAL'?45:65,strengthening=dir==='BUY'?h>prev:dir==='SELL'?h<prev:false;if(strengthening)score+=10;if(dir==='BUY'&&rv>=55&&rv<80)score+=10;if(dir==='SELL'&&rv<=45&&rv>20)score+=10;if((dir==='BUY'&&c.at(-1).c>=c.at(-1).o)||(dir==='SELL'&&c.at(-1).c<=c.at(-1).o))score+=5;return{dir,score:Math.min(100,score),rsi:rv,macdH:h,strengthening}}async function mtf(){let sym=$('#proAsset').value,status=$('#mtfStatus'),out=$('#mtfResult');status.textContent='Analisando MACD 20/30/60 em M5, M15 e H1...';try{let [a,b,c]=await Promise.all([kl(sym,'5m'),kl(sym,'15m'),kl(sym,'1h')]),s=[['M5',tfSignal(a)],['M15',tfSignal(b)],['H1',tfSignal(c)]],dirs=s.map(x=>x[1].dir),aligned=dirs.every(x=>x!=='NEUTRAL')&&new Set(dirs).size===1,dir=aligned?dirs[0]:'NEUTRAL';window.EPValidation=window.EPValidation||{mtf:{}};window.EPValidation.mtf=window.EPValidation.mtf||{};window.EPValidation.mtf[sym]={aligned,dir,score:Math.round(s.reduce((z,x)=>z+x[1].score,0)/3),ts:Date.now(),method:'MACD_20_30_60'};let verdict=aligned?(dir==='BUY'?'COMPRA — MACD M5/M15/H1 ALINHADO':'VENDA — MACD M5/M15/H1 ALINHADO'):'AGUARDAR — MACD sem alinhamento nos 3 timeframes';out.innerHTML=`<div class="confidence"><b>${verdict}</b></div>`+s.map(([t,x])=>`<div class=row><span>${t}</span><b>${x.dir} • ${x.score}/100 • MACD H ${fmt(x.macdH,6)} • RSI ${fmt(x.rsi,1)}</b></div>`).join('');status.textContent='Multi-timeframe MACD 20/30/60 atualizado.';window.dispatchEvent(new CustomEvent('mtf-updated',{detail:{asset:sym,aligned,dir,method:'MACD_20_30_60'}}))}catch(e){status.textContent='Falha: '+e.message}}function state(x){let d=window.EPDecision?.calc?.(x,'crypto');if(!d)return null;let atr=+x.atr||0,risk=atr*1.5,buy=d.dir==='BUY',entry=+x.price,stop=risk?(buy?entry-risk:entry+risk):NaN,target=risk?(buy?entry+risk*1.5:entry-risk*1.5):NaN;return{...d,entry,stop,target}}function renderOpportunities(){let el=$('#currentOpportunities'),map=window.CryptoApp?.getData?.();if(!el||!map)return;let rows=[...map.values()].map(x=>({x,s:state(x)})).filter(z=>z.s).sort((a,b)=>b.s.score-a.s.score).slice(0,6);el.innerHTML=rows.map(({x,s})=>`<div class="scan" style="cursor:default"><b>${x.key}/USDT</b><div class="score ${s.cls}">${s.score}</div><div><b>${s.decision}</b></div><small>${s.level}</small><div class=row><span>Preço</span><b>${fmt(s.entry,s.entry<10?4:2)}</b></div><div class=row><span>RSI 7</span><b>${fmt(s.rsi,1)} • ${s.extreme}</b></div><div class=row><span>CCI 14</span><b>${fmt(s.cci,1)}</b></div><div class=row><span>Stop paper</span><b>${fmt(s.stop,4)}</b></div><div class=row><span>Alvo paper</span><b>${fmt(s.target,4)}</b></div></div>`).join('')}function getPaper(){try{return JSON.parse(localStorage.getItem('ep_paper')||'[]')}catch{return[]}}function savePaper(a){localStorage.setItem('ep_paper',JSON.stringify(a))}function dedupe(a){let seen=new Set(),out=[];for(let i=a.length-1;i>=0;i--){let x=a[i],key=`${x.asset}|${x.dir}|${x.tf||'15m'}|${x.status||'OPEN'}`;if((x.status||'OPEN')==='OPEN'){if(seen.has(key))continue;seen.add(key)}out.push(x)}return out.reverse()}function label(x){return x.status==='TARGET'?'ALVO':x.status==='STOP'?'STOP':x.status==='FLIP'?'SINAL INVERTEU':x.status||'OPEN'}function card(x){let current=x.currentPrice??x.exitPrice,ret=Number.isFinite(+x.resultR)?`${x.resultR>0?'+':''}${fmt(x.resultR,2)} R`:'—';return `<div class="scan" style="cursor:default;margin-bottom:8px"><div class=row><span><b>${x.asset}</b> • ${x.dir} • ${x.tf||'15m'} • ${label(x)}</span><b>${new Date(x.time).toLocaleString('pt-BR')}</b></div><div class=row><span>Entrada</span><b>${fmt(x.entry,4)}</b></div><div class=row><span>Preço atual/saída</span><b>${fmt(current,4)}</b></div><div class=row><span>Score</span><b>${x.score??'—'}/100</b></div><div class=row><span>Stop</span><b>${fmt(x.stop,4)}</b></div><div class=row><span>Alvo</span><b>${fmt(x.target,4)}</b></div><div class=row><span>Resultado</span><b>${ret}</b></div></div>`}function paperLoad(){let a=dedupe(getPaper());savePaper(a);let el=$('#paperHistory');if(!el)return;let open=a.filter(x=>(x.status||'OPEN')==='OPEN').slice().reverse(),closed=a.filter(x=>(x.status||'OPEN')!=='OPEN').slice().reverse().slice(0,30);el.innerHTML=`<h4>OPERAÇÕES PAPER ATIVAS</h4>${open.length?open.map(card).join(''):'<p class="sub">Nenhuma operação paper ativa.</p>'}<h4>HISTÓRICO FINALIZADO</h4>${closed.length?closed.map(card).join(''):'<p class="sub">Ainda não há operações finalizadas.</p>'}`}function syncPaper(){let map=window.CryptoApp?.getData?.();if(!map)return;let tf=$('#tf')?.value||'15m',a=dedupe(getPaper()),now=Date.now();for(let x of a){if((x.status||'OPEN')!=='OPEN')continue;let d=map.get(x.asset);if(!d)continue;let p=+d.price,s=state(d);x.currentPrice=p;x.lastUpdate=now;let buy=x.dir==='BUY';if(Number.isFinite(+x.target)&&((buy&&p>=x.target)||(!buy&&p<=x.target))){x.status='TARGET';x.exitPrice=p;x.exitTime=now;x.resultR=1.5;continue}if(Number.isFinite(+x.stop)&&((buy&&p<=x.stop)||(!buy&&p>=x.stop))){x.status='STOP';x.exitPrice=p;x.exitTime=now;x.resultR=-1;continue}if(s?.tradeAllowed&&s.dir!==x.dir){x.status='FLIP';x.exitPrice=p;x.exitTime=now;let risk=Math.abs(x.entry-x.stop);x.resultR=risk?((buy?p-x.entry:x.entry-p)/risk):0}}for(let [asset,d] of map.entries()){let s=state(d);if(!s?.tradeAllowed)continue;let exists=a.some(x=>x.asset===asset&&(x.tf||'15m')===tf&&(x.status||'OPEN')==='OPEN');if(exists)continue;let recent=a.slice().reverse().find(x=>x.asset===asset&&x.dir===s.dir&&(x.tf||'15m')===tf);if(recent&&now-(recent.exitTime||recent.time)<5*60*1000)continue;a.push({asset,dir:s.dir,tf,entry:s.entry,currentPrice:s.entry,score:s.score,time:now,status:'OPEN',stop:s.stop,target:s.target,source:'AUTO'})}savePaper(a);paperLoad()}function paperAdd(){let asset=$('#proAsset').value,d=window.CryptoApp?.getData?.().get(asset),s=state(d);if(!s)return alert('Atualize os dados cripto primeiro.');if(!s.tradeAllowed)return alert(`${asset}: ${s.decision}. Entrada ainda não liberada pelo motor único.`);let tf=$('#tf')?.value||'15m',a=dedupe(getPaper());if(a.some(x=>x.asset===asset&&(x.tf||'15m')===tf&&(x.status||'OPEN')==='OPEN'))return alert(`Já existe operação PAPER aberta para ${asset} • ${tf}.`);a.push({asset,dir:s.dir,tf,entry:s.entry,currentPrice:s.entry,score:s.score,time:Date.now(),status:'OPEN',stop:s.stop,target:s.target,source:'MANUAL'});savePaper(a);paperLoad()}async function batch(){let status=$('#batchStatus'),out=$('#batchResult'),rows=[];status.textContent='Executando lote MACD 20/30/60 em M5/M15/H1...';for(let ai=0;ai<CRYPTO.length;ai++){let [sym,name]=CRYPTO[ai];for(let tf of ['5m','15m','1h']){try{let c=await kl(sym,tf,500),s=tfSignal(c),ret=(c.at(-1).c/c.at(-100).c-1)*100,quality=Math.max(0,Math.min(100,Math.round(s.score+Math.min(15,Math.abs(ret)))));rows.push({asset:name,tf,dir:s.dir,score:s.score,quality,ret})}catch{}}status.textContent=`Processando ${ai+1}/20...`}rows.sort((a,b)=>b.quality-a.quality);out.innerHTML=`<div class="bt-table-wrap"><table class="bt-table"><thead><tr><th>#</th><th>Ativo</th><th>TF</th><th>Direção</th><th>Score</th><th>Ranking</th></tr></thead><tbody>${rows.slice(0,30).map((x,i)=>`<tr><td>${i+1}</td><td>${x.asset}</td><td>${x.tf}</td><td>${x.dir}</td><td>${x.score}</td><td>${x.quality}</td></tr>`).join('')}</tbody></table></div>`;status.textContent=`Lote concluído: ${rows.length} combinações MACD.`}function eventRisk(){let v=$('#eventRisk').value,out=$('#eventResult');out.innerHTML=v==='high'?'<div class="rsi-alert overbought"><b>RISCO DE EVENTO ALTO — BLOQUEIA ENTRADA</b></div>':v==='medium'?'<div class="rsi-alert neutral"><b>RISCO MODERADO — observar com cautela</b></div>':'<div class="rsi-alert oversold"><b>RISCO NORMAL — filtro liberado</b></div>';window.dispatchEvent(new CustomEvent('event-risk-updated'))}function init(){let sel=$('#proAsset');if(!sel)return;sel.innerHTML=CRYPTO.map(([s,n])=>`<option value="${s}">${n}/USDT</option>`).join('');$('#runMtf').onclick=mtf;$('#runBatch').onclick=batch;$('#paperAdd').onclick=paperAdd;$('#paperClear').onclick=()=>{localStorage.removeItem('ep_paper');paperLoad()};$('#eventRisk').onchange=()=>{eventRisk();renderOpportunities();syncPaper()};window.addEventListener('crypto-data-updated',()=>{renderOpportunities();syncPaper()});window.addEventListener('mtf-updated',()=>{renderOpportunities();syncPaper()});eventRisk();paperLoad();renderOpportunities();syncPaper();setInterval(()=>{renderOpportunities();syncPaper()},5000)}setTimeout(init,300)})();